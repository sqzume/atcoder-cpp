#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < (n); i++)

int main() {
    ll a, b;
    cin >> a >> b;
    cout << b - a << endl;
    return 0;
}
