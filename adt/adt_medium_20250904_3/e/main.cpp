#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < (n); i++)

int main() {
    vector<ll> h(3), w(3);
    cin >> h[1] >> h[2] >> h[3];
    cin >> w[1] >> w[2] >> w[3];
    return 0;
}
